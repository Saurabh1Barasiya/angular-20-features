import { CustomstatusPipe } from './customstatus-pipe';

describe('CustomstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
