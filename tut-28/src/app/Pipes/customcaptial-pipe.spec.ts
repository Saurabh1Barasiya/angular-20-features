import { CustomcaptialPipe } from './customcaptial-pipe';

describe('CustomcaptialPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomcaptialPipe();
    expect(pipe).toBeTruthy();
  });
});
