import { Inputdetect } from './inputdetect';

describe('Inputdetect', () => {
  it('should create an instance', () => {
    const directive = new Inputdetect();
    expect(directive).toBeTruthy();
  });
});
