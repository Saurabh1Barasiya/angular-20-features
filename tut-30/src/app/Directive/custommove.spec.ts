import { Custommove } from './custommove';

describe('Custommove', () => {
  it('should create an instance', () => {
    const directive = new Custommove();
    expect(directive).toBeTruthy();
  });
});
